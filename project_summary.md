# ملخص مشروع بوت تحميل الفيديوهات (Telethon Userbot)

## نظرة عامة على المشروع
بوت شخصي (userbot) يعمل بحساب تيليجرام الشخصي (مو بوت عادي عبر Bot API) باستخدام مكتبة **Telethon**، يستمع فقط لمحادثة **Saved Messages** (رسائلي المحفوظة). لما ترسل رابط فيديو من أي موقع مدعوم (X، يوتيوب، فيسبوك، مواقع ثانية...)، يحمّله عبر **yt-dlp** ويرسله لك بأعلى جودة متاحة (أو الجودة اللي تحددها)، لغاية 2 جيجا بالملف.

**البنية التقنية:**
- **الاستضافة:** Render، عبر Docker
- **مكتبات رئيسية:** Telethon (التحكم بحساب تيليجرام)، yt-dlp (استخراج/تحميل الفيديو)
- **الكوكيز:** متغير بيئة `COOKIES_B64` (ملف cookies.txt مُرمّز Base64) للمحتوى اللي يتطلب تسجيل دخول
- **ملفات المشروع:** `telethon_bot.py` (الكود الرئيسي)، `requirements.txt`، `Dockerfile`، `generate_session.py`، `README.md`

## الميزات الحالية المتوفرة بالبوت
- تحميل فيديو من رابط، بأعلى جودة تلقائيًا أو جودة محددة (رقم بعد الرابط، مثل `720`)
- **اختيار جودة تفاعلي:** لو ما حددت رقم، يجيب قائمة الجودات الحقيقية المتوفرة للفيديو تحديدًا (بدون تحميل فعلي) ويسألك تختار برد برقم خلال 60 ثانية
- دعم أكثر من رابط بنفس الرسالة (يعالجهم بالترتيب)
- أوامر تحكم: `الغاء` (إيقاف تحميل جارٍ أو سؤال جودة مفتوح)، `تنظيف` (مسح ملفات مؤقتة متراكمة)، `اعادة تشغيل`، `حالة` (حالة السيرفر)، `احصائيات` (استهلاك الباندويدث)، `مساعدة`
- رسائل خطأ عربية واضحة (محتوى مقيّد بعمر، رابط غير مدعوم، محجوب جغرافيًا... إلخ)

## تسلسل المشاكل اللي انحلّت بهذي الجلسة (بالترتيب)

### 1. مشكلة يوتيوب الأساسية (من محادثة قديمة، خلفية فقط)
يوتيوب كان يرفض التحميل بمشاكل حظر IP سيرفرات Render (قيد معروف بمجتمع yt-dlp)، جُرّب تحديث الكوكيز وواجهات تطبيق بديلة (iOS/أندرويد) كحل مؤقت.

### 2. ميزة اختيار الجودة التفاعلي - خطأ `client.conversation()`
لما اتنفذت ميزة اختيار الجودة، طلع خطأ:
```
TypeError: Cannot cast InputPeerSelf to any kind of Peer
```
**السبب الجذري:** ميزة `client.conversation()` بمكتبة Telethon **غير متوافقة أصلاً** مع محادثة الحساب مع نفسه (Saved Messages) - أي كيان (entity) تمرره لها يتحول داخليًا لنوع خاص (`InputPeerSelf`) يفشل بنفس الخطأ دايمًا، بغض النظر عن طريقة الاستخدام.

**الحل المطبّق:** التخلي الكامل عن `client.conversation()`، واستبدالها بآلية انتظار يدوية عبر `asyncio.Future`:
- `ask_quality_choice()` يرسل رسالة السؤال عادي، يفتح `Future`، وينتظره لغاية 60 ثانية
- `handle_message` العام (المستمع لكل الرسائل) يتحقق أول شي: لو فيه سؤال جودة مفتوح، يعتبر أي رد (رقم أو "الغاء") إجابة عليه، بدل ما يعالجه كرابط جديد
- `handle_cancel` تعدّل عشان "الغاء" أثناء الانتظار يلغي السؤال نفسه كمان مو بس التحميلات الجارية
- شيلنا الرابط (URL) من رسالة "انتهى الوقت" تحديدًا، تحسبًا لمشكلة ثانية: أي رسالة يرسلها الحساب لنفسه بـ Saved Messages تولّد حدث "outgoing" يوصل لنفس `handle_message`، فلو كانت الرسالة فيها رابط كان ممكن يشغّل تحميل جديد بالغلط

**النتيجة:** الميزة اشتغلت تمام مع منصة X.

### 3. دقة ضعيفة + بدون قائمة خيارات بمواقع ثانية
بعد نجاح X، ظهرت مشكلة: بعض المواقع الثانية تنزّل بدقة ضعيفة **وبدون** ما تطلع قائمة اختيار الجودة أصلاً.

**التشخيص عبر السجلات (Logs):** السطر التشخيصي بالكود طلع:
```
الجودات المتوفرة بالمصدر: [] | المختارة فعليًا: NonexNone
```
يعني yt-dlp ما قدر يجيب أي بيانات جودة (height) من الرابط أصلاً - مو مجرد اختيار أقل جودة من عدة خيارات.

**السبب الجذري:** الرابط كان من دومين `xnxx-arabic.com` (نسخة/مرآة "عربية")، وهذا الدومين **غير مدعوم مباشرة** من yt-dlp، فيرجع لآلية استخراج عامة (generic extractor) تكتفي بأول رابط فيديو تلقاه من كود HTML، بدون قدرة على اكتشاف خيارات الجودة الفعلية بالموقع (اللي تحتاج طلب منفصل عادة).

**التأكيد:** جُرّب نفس الفيديو بالدومين الأساسي `xnxx.com` (بدون "-arabic") وطلعت قائمة الجودات طبيعي - أكّد إن المشكلة بالدومين تحديدًا مو بالكود نفسه.

**الحل المطبّق:** أضفنا قاموس "استبدال دومينات مرايا" بالكود (`MIRROR_DOMAIN_REWRITES`) ودالة `normalize_url()` تستبدل تلقائيًا أي رابط من `xnxx-arabic.com` بالدومين الأساسي `xnxx.com` **قبل** أي معالجة (سواء استعلام الجودة أو التحميل الفعلي). فأي رابط يوصل بالدومين "-arabic" يشتغل تلقائيًا بنفس جودة الدومين الأساسي، بدون ما يحتاج المستخدم يدوّر يدويًا.

## ملاحظة جانبية غير محلولة (سطحية، مو مؤكد أهميتها)
صار خطأ `MessageNotModifiedError` مرة وحدة بالسجلات (فشل تعديل رسالة الحالة لأن النص ما تغيّر)، مع بعض تحذيرات `FloodWaitError` متتالية أثناء رفع ملف. هذي أخطاء "متوقعة ومُتَجاهلة بتصميم الكود" (`safe_edit` مصممة تتجاهل `FloodWaitError`)، ما تمنع اكتمال العملية، بس تُذكر هنا للمرجعية لو تكرّرت بشكل مزعج لاحقًا.

## حالة الكود الحالية
كل التعديلات أعلاه (النقطتين 2 و3) مطبّقة على نسخة `telethon_bot.py` الحالية المرفقة بهذي المحادثة. **لازم:**
1. رفعها على GitHub بمكان النسخة القديمة
2. التأكد إن `Auto-Deploy` مفعّل بـ Render (كان انطفى تلقائيًا سابقًا بسبب Rollback بعد تعطل)، أو عمل `Manual Deploy → Deploy latest commit` يدويًا
3. انتظار اكتمال النشر (Live) قبل التجربة

## الوضع الحالي
آخر نقطة وصلنا لها: موقع pornhat.com مصنّف كغير مدعوم حاليًا (فخ مضاد للبوتات)، ومشكلة xHamster بانتظار تحديث من yt-dlp (الحل الجديد بالـ Dockerfile يجيبه تلقائيًا أول ما يتوفر). باقي المواقع (X، xnxx/xnxx-arabic، xvideos/xvideos-ar) تعمل بشكل طبيعي بجودة كاملة. مشكلة يوتيوب وPornHub (حظر IP) لسه بانتظار قرار المستخدم بخصوص البروكسي أو التشغيل من جهاز شخصي.

## نقاط مفتوحة من محادثات سابقة (لسه ما اتحلّت أو اتأجلت)
- قرار إضافة بروكسي IP سكني مدفوع لحل مشكلة حظر يوتيوب لـ IP سيرفرات Render (كان بانتظار قرار المستخدم، يقبل بعدم دعم يوتيوب مجانًا أو يضيف بروكسي)
- إمكانية إضافة خيار "قائمة دومينات مرايا" أوسع لو ظهرت مواقع ثانية بنفس مشكلة xnxx-arabic.com مستقبلًا

## 4. حظر PornHub بـ 403 Forbidden (مشكلة جديدة، بنفس فئة مشكلة يوتيوب)
جُرّب رابط PornHub (بعد ربط الكوكيز) وطلع:
```
yt_dlp.utils.DownloadError: ERROR: [PornHub] ... Unable to download webpage: HTTP Error 403: Forbidden
```
**السبب الجذري:** حظر على مستوى IP، مو مشكلة كوكيز — PornHub (زي يوتيوب) يحظر بشكل نشط نطاقات IP الخاصة بمزودي الاستضافة السحابية (Render، AWS، إلخ) لأنها معروفة كمصدر بوتات/سكرابينج، والحظر يصير قبل أي مرحلة تحتاج كوكيز أو مصادقة أصلاً (403 عند مجرد تحميل الصفحة).

**نفس الحل المطروح سابقًا ليوتيوب ينطبق هنا:** بروكسي IP سكني (residential proxy). ناقشنا خيارات مزودين (Webshare ~$2.80/جيجا، IPRoyal ~$1.75/جيجا، DataImpulse ~$1/جيجا، Decodo ~$2/جيجا) - التسعير عادة بالجيجابايت المنقول، ومهم ملاحظة إن تحميل الفيديو الفعلي (مو بس فتح الصفحة) يستهلك من نفس الحصة، فالتكلفة ترتبط بحجم الفيديوهات شهريًا.

**لا يوجد بروكسي سكني مجاني موثوق** - أي قائمة "بروكسي مجاني" عامة إما بطيئة/مزدحمة، أو محظورة مسبقًا من نفس المواقع، أو غير آمنة (خطر تسريب كوكيز حسابك). البديل: تجارب مجانية محدودة (بضع مئات ميجا/جيجا) يوفرها أغلب المزودين المدفوعين، تكفي للاختبار قبل الالتزام.

**حل بديل مجاني حقيقي (الأفضل قبل البروكسي):** تشغيل البوت من جهاز شخصي بالبيت بدل Render - يستخدم عنوان IP سكني حقيقي (عنوان الإنترنت المنزلي)، فيتفادى الحظر من جذوره بدون أي تكلفة، بشرط الجهاز يفضل شغّال ومتصل بالإنترنت باستمرار (عكس الاستضافة السحابية اللي تشتغل بدون اعتماد على جهاز المستخدم).

**الوضع الحالي:** بانتظار قرار المستخدم بين (تشغيل من جهاز شخصي / بروكسي مدفوع / تقبّل عدم دعم هذي المواقع حاليًا).

## 5. خطأ استخراج بموقع xHamster (خلل بمكتبة yt-dlp نفسها، مو بكودنا)
جُرّب رابط من xHamster وطلع:
```
yt_dlp.utils.ExtractorError: An extractor error has occurred. (caused by KeyError('videoModel'))
```
**السبب:** موقع xHamster غيّر بنية صفحته الداخلية (البيانات المُضمّنة بكود الصفحة تغيّر شكلها)، وآلية الاستخراج المخصصة لهذا الموقع بمكتبة yt-dlp (`xhamster.py`) صارت غير متوافقة مع البنية الجديدة - تبحث عن حقل (`videoModel`) ما عاد موجود بنفس المكان.

**هذا خلل بمكتبة yt-dlp نفسها (مشكلة معروفة الفئة: "الموقع غيّر بنيته وآلية الاستخراج صارت قديمة")، مو خلل بكودنا ولا بالإعدادات.** الحل ينتظر تحديث من فريق yt-dlp نفسه لآلية الاستخراج الخاصة بـ xHamster.

**خطوة تستاهل التجربة أولاً:** ملف `requirements.txt` يسحب yt-dlp من أحدث نسخة GitHub (`@master`) وقت البناء (build) - بس Render أحيانًا يستخدم طبقة بناء مخزّنة (cache) من نسخة أقدم بدل ما يعيد التحميل فعليًا. جرب **Render → Manual Deploy (⋮) → "Clear build cache & deploy"** لإجبار سحب أحدث نسخة yt-dlp، تحسبًا لو الفريق صلّح المشكلة مؤخرًا.

**الوضع الحالي:** غير محلولة - بانتظار نتيجة "Clear build cache & deploy"، أو تحديث لاحق من فريق yt-dlp لو استمرت المشكلة.

## 6. تحسين دائم: تحديث yt-dlp تلقائيًا بكل تشغيل (حل جذري لمشكلة الكاش بـ Render)
بدل ما نعتمد على تذكّر "Clear build cache & deploy" يدويًا كل مرة موقع ينكسر (زي حالة xHamster)، عدّلنا **Dockerfile** نفسه:
- الأمر النهائي (`CMD`) صار يسوي `pip install --upgrade` لـ yt-dlp من GitHub master **في كل مرة تشتغل الحاوية** (كل Deploy، وأي إعادة تشغيل فعلية للحاوية)، مو بس وقت بناء الصورة
- لو فشل التحديث لأي سبب (انقطاع شبكة لحظي)، البوت يكمل بالنسخة الموجودة بدل ما يتوقف بالكامل

**توضيح مهم:** UptimeRobot (اللي يبقي خدمة Render مستيقظة بالخطة المجانية) يسوي بس طلب HTTP عادي لفحص صحي - هذا **لا** يشغّل إعادة تشغيل للحاوية ولا يشغّل أمر `CMD` من جديد بكل بينج. التحديث يصير فقط عند Deploy فعلي أو إعادة تشغيل حقيقية، مو بكل مرة UptimeRobot يرسل طلب.

**الحالة:** الملف محدّث ومُرسل للمستخدم - يحتاج رفعه على GitHub وعمل Deploy (يلتقطه Render تلقائيًا لأن Dockerfile نفسه تغيّر).

## 7. إضافة دومين مرآة ثاني: xvideos-ar.com
نفس مشكلة xnxx-arabic.com بالضبط تكررت مع `xvideos-ar.com` (يرسل بدقة ضعيفة، وبحذف "-ar" من الدومين يشتغل طبيعي بجودة كاملة). **الحل:** أضفنا نفس الدومين لقاموس `MIRROR_DOMAIN_REWRITES` بالكود (`xvideos-ar.com` → `xvideos.com`)، بنفس آلية xnxx تمامًا. القاموس مصمم يتوسع بسهولة لأي دومين مرآة جديد يكتشفه المستخدم مستقبلًا (بس يرسل اسم الدومينين).

## 8. موقع pornhat.com - فخ مضاد للبوتات (ملف فيديو وهمي فاضي)
جُرّب رابط من pornhat.com (بدون كوكيز مربوطة لهذا الموقع تحديدًا) - القائمة التفاعلية لاختيار الجودة ظهرت بشكل طبيعي (720p/480p/360p/Auto)، لكن أيًا كان الخيار المُختار، الملف المُرسَل كان **978 بايت بس** (أقل من كيلوبايت)، بمدة 0:00، وما يفتح ولا له صورة مصغّرة بتيليجرام.

**التشخيص:** هذا مو نقص بيانات جودة (زي xnxx-arabic) ومو خطأ استخراج (زي xHamster) - هذا **ملف وهمي/فاسد متعمّد** يرجّعه الموقع نفسه كفخ مضاد للبوتات (anti-bot trap) لما يكتشف إن الطلب جاي من أداة آلية مو متصفح حقيقي (بسبب غياب كوكيز جلسة، Referer صحيح، أو توقيع JavaScript)، بدل ما يرجّع خطأ صريح زي 403.

**القرار:** بما إنه تكتيك متعمّد من الموقع (مو مجرد نقص بيانات نقدر نحله بالكود)، اتفقنا نتقبّل عدم دعم هذا الموقع حاليًا (نفس وضع xHamster) بدل محاولة تحايل غير موثوقة. اقتراح غير مؤكد النجاح: إضافة كوكيز عادية (حتى بدون تسجيل دخول) لهذا الموقع تحديدًا قد تساعد، لكن غير مضمون لو الموقع يستخدم فحوصات JavaScript إضافية ما يقدر yt-dlp يجتازها أصلاً.

**الحالة:** غير محلولة، ومصنّفة كموقع غير مدعوم حاليًا (بانتظار قرار المستخدم لو يبي يجرب إضافة كوكيز رغم عدم ضمان النجاح).

## 9. دعم استخدام البوت من أرقام أخرى عبر مجموعة خاصة (GROUP_CHAT_ID)
طلب المستخدم إمكانية إضافة حساب البوت لمجموعة خاصة تضم أرقامه الأخرى، بحيث أي رابط يُرسل من أي رقم بالمجموعة يُعامَل بنفس طريقة Saved Messages تمامًا (تحميل، قائمة اختيار جودة، أوامر تحكم)، والفيديو يوصل لنفس المجموعة.

**التعديلات المطبّقة على `telethon_bot.py`:**
- متغير بيئة اختياري جديد `GROUP_CHAT_ID` (رقم المجموعة)، يُبنى منه `ALLOWED_CHATS = ["me", GROUP_CHAT_ID]`
- دالة `is_allowed_trigger()` جديدة تميّز الحالتين:
  - Saved Messages: لازم `out=True` (نفس المنطق القديم)
  - المجموعة الخاصة: لازم `out=False` (رسالة واردة من رقم ثاني)، عشان رسائل حالة/ردود البوت نفسه بالمجموعة (اللي تطلع `out=True` لأنها من نفس حساب البوت) ما تُعاد معالجتها كطلب جديد
- كل الـ decorators (`الغاء`، `تنظيف`، `اعادة تشغيل`، `تأكيد إعادة التشغيل`، `حالة`، `احصائيات`، `مساعدة`، والمعالج العام `handle_message`) عدّلت من `chats="me", outgoing=True` إلى `chats=ALLOWED_CHATS` + فحص `is_allowed_trigger()` يدوي بأول كل دالة
- `pending_quality_future` (متغير عام واحد) تحوّل لقاموسين مفهرسين بـ `chat_id` (`pending_quality_futures`, `pending_quality_options`) - عشان سؤال جودة مفتوح بمحادثة (مثلاً المجموعة) ما يتعارض مع سؤال ثاني مفتوح بمحادثة ثانية (Saved Messages) بنفس الوقت
- `ask_quality_choice()` صار يستقبل `chat_id` ويرسل السؤال لنفس المحادثة اللي جا منها الرابط، بدل ما كان مثبّت دايمًا يرسل لـ "me"
- إرسال الفيديو النهائي (`client.send_file(...)`) تغيّر من مثبّت على `"me"` إلى `event.chat_id` - عشان الفيديو يوصل لنفس مكان الطلب (Saved Messages أو المجموعة)، مو دايمًا لك بس

**قيد معروف مقصود (تبسيط):** أوامر التحكم (`الغاء`، `اعادة تشغيل`، إلخ) تشتغل على مستوى البوت كله (متغيرات عامة زي `active_operations`)، مو منفصلة لكل محادثة - يعني "الغاء" من المجموعة يوقف أي تحميل جارٍ حتى لو بدأ من Saved Messages، والعكس صحيح. مقبول لأن الاستخدام شخصي بالكامل (نفس المالك لكل الأرقام).

**ملف جديد:** `get_group_id.py` - سكربت مساعد (بنفس أسلوب `generate_session.py`) يطبع قائمة كل محادثات الحساب مع `chat_id` الخاص فيها، لتسهيل إيجاد رقم المجموعة الجديدة بعد إنشائها.

**تحديث `README.md`:** أضيف قسم 7 كامل يشرح خطوات الإعداد (إنشاء مجموعة، إضافة الأرقام، تشغيل `get_group_id.py`، ضبط `GROUP_CHAT_ID` على Render).

**الوضع الحالي:** الكود جاهز، يحتاج من المستخدم:
1. إنشاء المجموعة الخاصة وإضافة الأرقام لها يدويًا بتيلجرام
2. تشغيل `get_group_id.py` لإيجاد `chat_id` الصحيح
3. ضبط متغير `GROUP_CHAT_ID` على Render، ثم رفع الملفات المعدّلة على GitHub (سيلتقطها Render تلقائيًا كـ Deploy جديد)

## ملخص الملفات المُرسلة بهذي الجلسة
- **telethon_bot.py** - آخر نسخة (اختيار الجودة اليدوي + استبدال دومينات xnxx-arabic وxvideos-ar + دعم مجموعة خاصة عبر GROUP_CHAT_ID)
- **Dockerfile** - آخر نسخة (تحديث yt-dlp تلقائي بكل تشغيل حاوية)
- **get_group_id.py** - سكربت جديد لإيجاد chat_id الخاص بأي محادثة/مجموعة
- **README.md** - محدّث بقسم شرح إعداد المجموعة الخاصة (قسم 7)
- **project_summary.md** - هذا الملف نفسه (يُحدَّث بكل مرة تطلب ملخص جديد)
